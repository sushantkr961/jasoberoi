/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "aaah0mnbncqtinas.public.blob.vercel-storage.com",
      "jasoberoi.ca",
    ],
  },
};

export default nextConfig;
