import React from 'react'

type Props = {}

const NavMobile = (props: Props) => {
    return (
        <nav className="nav-mobile">
            <ul className="">
            </ul>
        </nav>
    )
}

export default NavMobile